trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - main

stages:
  - stage: Build
    displayName: CI
    jobs:
      - job: ci
        displayName: Build, Test & Verify
        pool:
          name: selfhost-linux
        steps:
          - checkout: self
            fetchDepth: 1

          - script: chmod +x mvnw
            displayName: Ensure mvnw executable

          - script: ./mvnw -B -ntp verify
            displayName: Maven verify (tests + spotless + jacoco)
            env:
              DOCKER_HOST: unix:///var/run/docker.sock
              TESTCONTAINERS_RYUK_DISABLED: "true"

  - stage: Deploy
    displayName: CD (self-host)
    dependsOn: Build
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
    jobs:
      - deployment: deploy_local
        displayName: Deploy with Docker Compose
        environment: cicd-local
        pool:
          name: selfhost-linux
        strategy:
          runOnce:
            deploy:
              steps:
                - checkout: self
                  fetchDepth: 1

                - script: chmod +x mvnw
                  displayName: Ensure mvnw executable

                - script: ./mvnw -B -ntp -q -DskipTests package
                  displayName: Build jar (tests already ran in CI)

                - script: docker compose -f deploy/compose.yaml build
                  displayName: Docker build

                - script: docker compose -f deploy/compose.yaml up -d --remove-orphans
                  displayName: Docker up

                - script: bash deploy/smoke.sh
                  displayName: Smoke tests

                - script: docker compose -f deploy/compose.yaml logs --no-color
                  displayName: Logs (always)
                  condition: always()
